# 家具の英語名

両ツールの家具一覧の日本語名の隣に「英語名」の検索列を追加しています。英語名の検索・値選択と、日本語の検索条件を併用できます。状態操作の家具名付き取り込みでも英語名を使用できます（英字の大小は不問）。出力と保存の識別には従来どおり日本語名を使います。

英語名は [Twisted Wonderland Wiki の Furniture テーブル](https://twistedwonderland.wiki.gg/wiki/Special:CargoTables/Furniture) の表記です。公式英語版の名称であることを保証するものではなく、翻訳は生成していません。出典ページは `furniture_english.json` の各項目に保持しています。

## 照合

日本語名を Unicode NFKC 正規化して空白を除き、完全一致したものだけを採用します。同名で家具IDや英語名が異なるもの、同一IDに異なる日本語名があるもの、未掲載・空欄・仮の名称は自動採用しません。英語名が未対応の家具と手入力家具は空欄のままです。既存の `data.csv` の列・行番号・計算データは変更しません。

## 取得データの検証

一覧の名前セル全体ではなく、家具の通常ページへのリンク本文だけを英語名として読みます。画像未登録時の `Special:Upload`・`File:` リンク、画像だけのリンク、リンク外の文字は対象外です。異なる名前のリンクが同じセルにある場合も採用しません。

出力時には `File:`・画像拡張子・URL・内部識別子・プレースホルダーを拒否し、既存辞書にも同じ検査を適用します。日本語の「の壁」に英語の `Floor`、「の床」に `Wall` が対応する取得元の矛盾も未確認扱いにします。文字列を削って名前を推測する補正は行いません。

2026-09-26 の全件検査で画像リンクの混入119件を確認。取得済みHTMLの家具名リンクとIDを再照合し117件を修正しました。取得元で壁・床の対応が逆になっている「クリスマス・タウンの壁／床」2件は空欄としています。現在の対応数は1,449件、未対応は417件です。この検査は表記・対応の異常検出であり、全英語名を公式ゲーム内表記と照合したものではありません。

## 更新

```sh
python update_furniture_english.py
python -m unittest test_furniture_english.py
node --test test_room_state.js
```

Python の標準ライブラリのみを使用します。`furniture_english.json` が配布用の辞書です。閲覧者はこのファイルだけを取得し、外部 wiki に通信しません。`main.py` のスプレッドシート更新とも独立しているので、英語名が上書きされることはありません。

`.github/workflows/furniture-english.yml` は毎週水曜 04:37（日本時間）に実行予定です。手動実行にも対応しています。デフォルトブランチに配置され、Actions と bot による書き込みが許可されている必要があります。保護ブランチ等で push が拒否された場合は実行が失敗として表示されます。GitHub の都合で定刻から遅れる場合があり、公開リポジトリで長期に活動がない場合は定期実行が停止される場合があります。[GitHub の schedule 仕様](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#schedule)

取得先への負荷を抑えるため、次の動作にしています。

- robots.txt により禁止されている API・特殊ページを定期取得しない。繰り返される User-agent グループの禁止指定も統合して判定。
- 通常の `Furniture_Data` 一覧と `Permanent_Furniture` を週1回取得。照合済み家具は一覧の家具IDで英語名を更新する。一覧に掲載されたイベント・キャラクターの通常ページは週4ページずつ順番に取得し、日本語名と英語名を補完する。
- 1回あたり robots.txt を含め最大7リクエスト。画像・スクリプト・CSSは取得しない。並列取得なし、間隔は15秒以上（robots.txt の指定が長い場合はその指定）。
- HTTP キャッシュを Actions cache に保持。ETag／Last-Modified が返る場合は条件付きGETを使用。
- 403・429・503・タイムアウト時は即終了し、自動再試行しない。
- 今回取得した全ページの検証が成功してから辞書を置換。対象外のページに由来する英語名は保持。異常に少ない取得件数・形式変更では既存ファイルを保持して失敗。
- 内容が変わったときだけ辞書1ファイルをコミット。更新日時だけによるコミットは作成しない。

初期辞書は今回確認した Cargo テーブルの日英対応から作成しました。定期取得は通常ページのみです。イベント・キャラクターのページは分割して巡回するため、新規家具の英語名が必ず翌週に追加されるわけではありません。未掲載・同名の曖昧な家具は空欄のままになります。

取得方式・robots.txt の確認日：2026-09-26。
